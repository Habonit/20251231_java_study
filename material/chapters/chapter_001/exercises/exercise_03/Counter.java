package chapters.chapter01.exercises.exercise_03;

/**
 * 연습 문제 3: Counter 클래스 정의
 *
 * 참조값(Reference)이란?
 * - 객체가 메모리에 생성되면 그 위치를 가리키는 주소가 반환된다.
 * - 이 주소를 참조값이라고 한다.
 * - 변수에는 객체 자체가 아니라 이 참조값이 저장된다.
 */
public class Counter {

    // TODO: 멤버 변수(필드)를 정의하세요
    // - count (int): 카운트 값
    int count;
}

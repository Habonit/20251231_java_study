# 챕터 2: 기본형과 참조형.pdf

> 학습 날짜: 2026.01.02.

## 범위

- 교안: `material/pdf/2. 기본형과 참조형.pdf`
- 페이지: 1-33

---

## 강의 내 질문

> 수업 중 궁금했던 점들. **연습문제 생성의 주요 근거로 사용됨.**

- int, long, double의 차이
- 자바에서 public static void 라고 선언하는 것의 의도를 잘 모르겠음 어떤 매서드에는 static void만 들어가고 어떨 때엔 static만 들어가는데? 각각을 python에선 각각 무슨 기능으로 볼 수 있는지 비교해서 답변해도 됨. void는 return이 없을 때만 쓰는 거고, static은 self 변수를 안 받을 때인가? public은 언제 쓰는 거고?
- 자바에서 스코프의 범위는 어떻게 정해지는지?
- gc는 gpu 메모리에 남아있는 것도 모두 잘 지우는가? 

---

## 노트 작성

> 수업 내용 정리. 자유롭게 작성.

- 기본형엔 실제값이 변수에 들어가지만 참조형엔 실제 객체의 위치, 즉 참조값이 들어간다. 

- 기본형은 자바가 기본으로 제공하는 데이터 타입이다. 이러한 기본형은 개발자가 새로 정의할 수 없으며 개발자는 참조형인 클래스만 직접 정의할 수 있다. 

- String은  클래스를 쓰는 참조형이지만, 이를 기본형처럼 다룰 수 있게 편의 기능을 제공하는 것 뿐이다. 즉 string을 변수에 할당하면 참조값이 들어가는 것.

- class와 method를 사용하면 반복되는 코드를 줄일 수 있다. 또 라인도 줄일 수 있다. 

- package ref 라고 하면 어떻게 변화되는 것인가?

- 멤버변수 = class에서 선언한 것, 지역 변수는 인스턴스 내에서 선언된 것. 

- gc : 아무도 참조하지 않는 데이터를 메모리에서 제거하는 것. 

- 객체를 참조할 땐, dot을 사용한다. 
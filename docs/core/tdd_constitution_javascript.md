# 테스트 원칙 및 방법론 - JavaScript

## 개요

이 문서는 **TDD(Test-Driven Development)**를 JavaScript 환경에서 실천하기 위한 완전한 방법론을 제공한다.

### 이 문서를 숙지하면 할 수 있는 것

| 역량 | 설명 | 관련 섹션 |
|:-----|:-----|:-----|
| **Red-Green-Refactor 사이클 실천** | 실패하는 테스트 작성 → 통과하는 코드 구현 → 리팩토링의 반복 | Part 1, Section 1 |
| **효과적인 테스트 케이스 설계** | 경계값 분석, 동치 분할로 누락 없는 테스트 케이스 도출 | Part 1, Section 3 |
| **테스트 더블 활용** | Mock, Stub, Spy를 Jest로 구현 | Part 1, Section 4 |
| **단위 테스트 작성** | describe/test 패턴, Jest matcher 활용 | Part 2 |
| **통합 테스트 작성** | Supertest, 실제 의존성 테스트 | Part 3 |
| **E2E 테스트 설계** | Playwright, Cypress 활용 | Part 4 |

### Red → Green → Refactor 달성 경로

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          TDD 사이클 달성 경로                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   🔴 RED (실패하는 테스트 작성)                                               │
│   ├─ Part 1, Section 3: 테스트 케이스 설계 기법으로 "무엇을 테스트할지" 결정     │
│   ├─ Part 2: 단위 테스트 작성 규칙으로 "어떻게 테스트할지" 결정                 │
│   └─ Part 3, 4: 통합/E2E 테스트로 "전체 흐름을 어떻게 검증할지" 결정            │
│                                                                             │
│                              ▼                                              │
│                                                                             │
│   🟢 GREEN (테스트를 통과하는 최소한의 코드 작성)                               │
│   ├─ Part 1, Section 1.2: "최소한의 코드"란 테스트만 통과하는 코드              │
│   ├─ Part 1, Section 4: Jest mock으로 외부 의존성 격리하며 구현                │
│   └─ Part 2, Section 5.3: Arrange-Act-Assert 패턴으로 구현 결과 검증          │
│                                                                             │
│                              ▼                                              │
│                                                                             │
│   🔵 REFACTOR (코드 개선, 테스트는 계속 통과)                                  │
│   ├─ Part 1, Section 2: beforeEach로 중복 제거                               │
│   ├─ Part 2, Section 7: 테스트 우선순위로 중요한 것부터 개선                    │
│   └─ 모든 Part: 테스트가 통과하면 안전하게 리팩토링 가능                        │
│                                                                             │
│                              ▼                                              │
│                         (다음 기능으로 반복)                                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### TDD 숙련도 체크리스트

- [ ] Red-Green-Refactor 사이클을 설명하고 실천할 수 있다
- [ ] 테스트를 먼저 작성하고, 그 테스트를 통과하는 코드를 구현할 수 있다
- [ ] 경계값 분석과 동치 분할로 테스트 케이스를 설계할 수 있다
- [ ] Jest mock, spy, fn의 차이를 알고 적절히 사용할 수 있다
- [ ] Arrange-Act-Assert 패턴으로 테스트를 구조화할 수 있다
- [ ] beforeEach, afterEach를 활용하여 테스트 간 중복을 제거할 수 있다
- [ ] 단위/통합/E2E 테스트의 차이를 알고 상황에 맞게 선택할 수 있다
- [ ] 테스트 피라미드 비율(Unit 70% : Integration 25% : E2E 5%)을 이해한다

---

## 목차

- [Part 1: TDD 방법론](#part-1-tdd-방법론)
- [Part 2: 단위 테스트 규칙](#part-2-단위-테스트-규칙)
- [Part 3: 통합 테스트 규칙](#part-3-통합-테스트-규칙)
- [Part 4: E2E 테스트 규칙](#part-4-e2e-테스트-규칙)

---

# Part 1: TDD 방법론

## 1. TDD란?

**TDD(Test-Driven Development, 테스트 주도 개발)**는 테스트를 먼저 작성하고, 그 테스트를 통과하는 코드를 작성하는 개발 방법론이다.

### 1.1 핵심 철학

> "동작하는 깔끔한 코드(Clean code that works)"를 목표로 한다.

- **테스트가 개발을 이끈다**: 코드를 작성하기 전에 "이 코드가 무엇을 해야 하는가?"를 테스트로 먼저 정의
- **작은 단위로 반복**: 한 번에 하나의 기능만 테스트하고 구현
- **리팩토링 안전망**: 테스트가 있으면 코드를 안전하게 개선할 수 있음

### 1.2 Red-Green-Refactor 사이클

TDD의 핵심은 **3단계 사이클**을 반복하는 것이다:

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│    ┌─────────┐      ┌─────────┐      ┌───────────┐         │
│    │   Red   │ ───► │  Green  │ ───► │ Refactor  │ ──┐     │
│    │ (실패)  │      │ (통과)  │      │  (개선)   │   │     │
│    └─────────┘      └─────────┘      └───────────┘   │     │
│         ▲                                            │     │
│         └────────────────────────────────────────────┘     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

| 단계 | 설명 | 예시 |
|:-----|:-----|:-----|
| **Red** | 실패하는 테스트 작성 | `test('add 함수')` 작성 → 실행 → 실패 (함수가 없으니까) |
| **Green** | 테스트를 통과하는 최소한의 코드 작성 | `function add(a, b) { return a + b }` 구현 → 테스트 통과 |
| **Refactor** | 코드 개선 (테스트는 계속 통과해야 함) | 중복 제거, 네이밍 개선, 구조 정리 |

### 1.3 TDD 예시

```javascript
// 1단계: Red - 실패하는 테스트 작성
describe('calculateDiscount', () => {
  test('10% 할인이 적용되어야 한다', () => {
    expect(calculateDiscount(1000, 10)).toBe(900);
  });
});
// → 실행하면 실패 (calculateDiscount 함수가 없음)

// 2단계: Green - 테스트를 통과하는 최소한의 코드
function calculateDiscount(price, percent) {
  return price - (price * percent / 100);
}
// → 테스트 통과

// 3단계: Refactor - 코드 개선
/**
 * 가격에서 할인율만큼 차감한 금액을 반환.
 * @param {number} price - 원가
 * @param {number} percent - 할인율 (0-100)
 * @returns {number} 할인 적용 금액
 * @throws {Error} 할인율이 0-100 범위를 벗어나면 에러 발생
 */
function calculateDiscount(price, percent) {
  if (percent < 0 || percent > 100) {
    throw new Error('할인율은 0~100 사이여야 합니다');
  }
  const discountAmount = price * percent / 100;
  return price - discountAmount;
}
// → 테스트 여전히 통과, 코드는 더 견고해짐
```

---

## 2. TDD 적용 시나리오

### 2.1 현실적인 TDD

| 상황 | 적용 방법 |
|:-----|:---------|
| **새 기능 추가** | 테스트 먼저 작성 → 기능 구현 → 리팩토링 |
| **버그 수정** | 버그를 재현하는 테스트 작성 → 수정 → 테스트 통과 확인 |
| **기존 코드** | 현재 동작을 검증하는 테스트 작성 (Characterization Test) |

### 2.2 테스트 격리 (Test Isolation)

각 테스트는 **다른 테스트에 영향을 주거나 받지 않아야** 한다.

```javascript
// Bad: 테스트 간 상태 공유
let counter = 0;

test('increment', () => {
  counter += 1;
  expect(counter).toBe(1);  // 통과
});

test('increment again', () => {
  counter += 1;
  expect(counter).toBe(1);  // 실패! (counter가 이미 1이므로 2가 됨)
});

// Good: 각 테스트가 독립적
describe('counter', () => {
  let counter;

  beforeEach(() => {
    counter = 0;  // 매 테스트마다 초기화
  });

  test('increment', () => {
    counter += 1;
    expect(counter).toBe(1);
  });

  test('increment again', () => {
    counter += 1;
    expect(counter).toBe(1);  // 통과
  });
});
```

---

## 3. 테스트 케이스 설계 기법

### 3.1 경계값 분석 (Boundary Value Analysis)

```javascript
// 예: 나이가 0~120 사이여야 유효한 경우
function isValidAge(age) {
  return age >= 0 && age <= 120;
}

describe('isValidAge', () => {
  test('경계값 테스트', () => {
    // 경계 안쪽 (유효)
    expect(isValidAge(0)).toBe(true);     // 최소 경계
    expect(isValidAge(120)).toBe(true);   // 최대 경계

    // 경계 바깥 (무효)
    expect(isValidAge(-1)).toBe(false);   // 최소 경계 -1
    expect(isValidAge(121)).toBe(false);  // 최대 경계 +1
  });
});
```

### 3.2 동등 분할 (Equivalence Partitioning)

```javascript
// 예: 점수에 따른 등급 계산
function getGrade(score) {
  if (score >= 90) return 'A';
  if (score >= 80) return 'B';
  if (score >= 70) return 'C';
  return 'F';
}

describe('getGrade', () => {
  test('동등 분할 테스트', () => {
    expect(getGrade(95)).toBe('A');  // 90-100 파티션
    expect(getGrade(85)).toBe('B');  // 80-89 파티션
    expect(getGrade(75)).toBe('C');  // 70-79 파티션
    expect(getGrade(50)).toBe('F');  // 0-69 파티션
  });
});
```

### 3.3 테스트 케이스 우선순위

| 우선순위 | 케이스 유형 | 이유 |
|:---------|:-----------|:-----|
| P0 | 정상 케이스 (Happy Path) | 기본 기능이 동작해야 함 |
| P1 | 경계값 케이스 | 버그가 자주 발생하는 지점 |
| P2 | 에러 케이스 | 예외 상황 처리 검증 |
| P3 | 극단적 케이스 | null, undefined, 매우 큰 입력 등 |

---

## 4. 테스트 더블 (Test Double)

### 4.1 테스트 더블이란?

실제 객체 대신 **테스트용 가짜 객체**를 사용하여 외부 의존성을 격리한다.

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        테스트 더블의 개념                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   실제 시스템                           테스트 환경                       │
│   ┌─────────────┐                      ┌─────────────┐                  │
│   │   내 코드   │                      │   내 코드   │                  │
│   └──────┬──────┘                      └──────┬──────┘                  │
│          │ 호출                               │ 호출                     │
│          ▼                                    ▼                         │
│   ┌─────────────┐                      ┌─────────────┐                  │
│   │  실제 DB    │  ─────────────►      │  Mock DB    │  ← 테스트 더블   │
│   │  실제 API   │      대체            │  Mock API   │                  │
│   └─────────────┘                      └─────────────┘                  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.2 Jest를 활용한 테스트 더블

| 종류 | Jest 구현 | 사용 시점 |
|:-----|:----------|:---------|
| **Mock** | `jest.fn()`, `jest.mock()` | 호출 여부/횟수 검증 |
| **Spy** | `jest.spyOn()` | 실제 동작 + 호출 추적 |
| **Stub** | `mockReturnValue()`, `mockResolvedValue()` | 미리 정해진 값 반환 |

### 4.3 Mock 예시

```javascript
// userService.js
class UserService {
  constructor(userRepository) {
    this.userRepository = userRepository;
  }

  async getUserName(userId) {
    const user = await this.userRepository.findById(userId);
    return user?.name;
  }
}

// userService.test.js
describe('UserService', () => {
  /**
   * 대상: src/userService.js - getUserName()
   * 의도: 사용자 조회 시 이름을 정상 반환하는지 검증
   */
  test('getUserName - 사용자 이름 반환', async () => {
    // Arrange: Mock 설정
    const mockRepository = {
      findById: jest.fn().mockResolvedValue({ id: 1, name: '홍길동' })
    };
    const service = new UserService(mockRepository);

    // Act
    const result = await service.getUserName(1);

    // Assert
    expect(result).toBe('홍길동');
    expect(mockRepository.findById).toHaveBeenCalledWith(1);
    expect(mockRepository.findById).toHaveBeenCalledTimes(1);
  });
});
```

### 4.4 모듈 Mock

```javascript
// api.js
export async function fetchUser(id) {
  const response = await fetch(`/api/users/${id}`);
  return response.json();
}

// userController.test.js
jest.mock('./api');
import { fetchUser } from './api';
import { getUserData } from './userController';

describe('getUserData', () => {
  test('API 호출 후 데이터 반환', async () => {
    // Arrange
    fetchUser.mockResolvedValue({ id: 1, name: '홍길동' });

    // Act
    const result = await getUserData(1);

    // Assert
    expect(result.name).toBe('홍길동');
  });
});
```

### 4.5 언제 테스트 더블을 사용하나?

| 상황 | 사용 여부 | 이유 |
|:-----|:---------|:-----|
| 외부 API 호출 | O | 네트워크 의존성 제거, 비용 절감 |
| 데이터베이스 접근 | O 또는 X | 단위 테스트는 Mock, 통합 테스트는 실제 DB |
| 시간 의존 로직 | O | jest.useFakeTimers()로 시간 Mock |
| 내부 유틸 함수 | X | 실제 구현 사용 (과도한 Mock 지양) |

---

## 5. 테스트 유형 선택 가이드

### 5.1 테스트 피라미드

```
                    ┌───────┐
                    │  E2E  │  ← 5% (핵심 시나리오만)
                   ─┴───────┴─
                  ┌───────────┐
                  │   통합    │  ← 25% (주요 워크플로우)
                 ─┴───────────┴─
                ┌───────────────┐
                │    단위       │  ← 70% (대부분의 테스트)
                └───────────────┘
```

---

# Part 2: 단위 테스트 규칙

## 6. 핵심 개념

### 6.1 beforeEach / afterEach

테스트 실행 전후에 필요한 데이터나 환경을 준비한다.

```javascript
describe('Calculator', () => {
  let calculator;

  beforeAll(() => {
    // 전체 테스트 그룹에서 한 번만 실행
    console.log('테스트 시작');
  });

  beforeEach(() => {
    // 각 테스트 전마다 실행
    calculator = new Calculator();
  });

  afterEach(() => {
    // 각 테스트 후마다 실행
    calculator = null;
  });

  afterAll(() => {
    // 전체 테스트 그룹 종료 시 한 번만 실행
    console.log('테스트 종료');
  });

  test('add', () => {
    expect(calculator.add(2, 3)).toBe(5);
  });
});
```

| 함수 | 실행 시점 | 사용 사례 |
|:-----|:---------|:---------|
| `beforeAll` | 그룹당 한 번 (테스트 시작 전) | DB 연결, 무거운 리소스 초기화 |
| `beforeEach` | 각 테스트 전 | 테스트 객체 초기화, Mock 설정 |
| `afterEach` | 각 테스트 후 | 리소스 정리, Mock 초기화 |
| `afterAll` | 그룹당 한 번 (테스트 종료 후) | DB 연결 해제 |

### 6.2 테스트 건너뛰기

```javascript
// 특정 테스트 건너뛰기
test.skip('미완성 테스트', () => {
  // 건너뜀
});

// 특정 테스트만 실행
test.only('이 테스트만 실행', () => {
  // 실행됨
});

// 조건부 건너뛰기
const isCI = process.env.CI === 'true';
(isCI ? test.skip : test)('로컬에서만 실행', () => {
  // CI 환경에서는 건너뜀
});
```

### 6.3 테스트 커버리지

```bash
# 커버리지 측정
npm test -- --coverage

# 특정 파일만 측정
npm test -- --coverage --collectCoverageFrom='src/**/*.js'
```

**jest.config.js 설정:**

```javascript
module.exports = {
  collectCoverage: true,
  coverageDirectory: 'coverage',
  coverageThreshold: {
    global: {
      branches: 80,
      functions: 80,
      lines: 80,
      statements: 80
    }
  }
};
```

---

## 7. 테스트 작성 규칙

### 7.1 양방향 추적성 (Bidirectional Traceability)

**테스트 → 소스 코드**

```javascript
/**
 * 대상: src/calculator.js:45 - add()
 * 의도: 양수 두 개를 더했을 때 올바른 결과 반환 검증
 */
test('add - 양수 두 개 덧셈', () => {
  // ...
});
```

**소스 코드 → 테스트**

```javascript
/**
 * 두 숫자를 더한 결과를 반환.
 *
 * @Test tests/calculator.test.js::add - 양수 두 개 덧셈
 */
function add(a, b) {
  return a + b;
}
```

### 7.2 Arrange-Act-Assert 패턴

모든 테스트는 **AAA 패턴**을 따라 구조화한다.

```javascript
/**
 * 대상: src/utils.js:234 - filterList()
 * 의도: 조건에 맞는 항목만 필터링되는지 검증
 */
test('filterList - 조건에 맞는 항목 필터링', () => {
  // Arrange: 테스트 데이터 및 환경 준비
  const items = [1, 2, 3, 4, 5];
  const threshold = 3;

  // Act: 테스트 대상 함수 실행
  const result = filterList(items, x => x >= threshold);

  // Assert: 결과 검증
  expect(result).toEqual([3, 4, 5]);
});
```

### 7.3 비동기 테스트

```javascript
// async/await 사용 (권장)
test('비동기 함수 테스트', async () => {
  const result = await fetchData();
  expect(result).toBeDefined();
});

// Promise 반환
test('Promise 테스트', () => {
  return fetchData().then(result => {
    expect(result).toBeDefined();
  });
});

// done 콜백 (레거시)
test('콜백 테스트', done => {
  fetchDataCallback((err, result) => {
    expect(result).toBeDefined();
    done();
  });
});
```

---

## 8. 테스트 구조 규칙

### 8.1 네이밍 규칙

**파일명**: `{모듈명}.test.js`
- `calculator.test.js`
- `userService.test.js`

**테스트명**: `{함수명} - {시나리오}`
- `add - 양수 두 개 덧셈`
- `parse - 유효하지 않은 입력 시 에러`

### 8.2 describe를 활용한 그룹화

```javascript
describe('UserService', () => {
  describe('findById', () => {
    test('유효한 ID로 조회 시 사용자 반환', () => {
      // ...
    });

    test('존재하지 않는 ID로 조회 시 null 반환', () => {
      // ...
    });
  });

  describe('createUser', () => {
    test('유효한 입력으로 사용자 생성', () => {
      // ...
    });

    test('필수 필드 누락 시 에러', () => {
      // ...
    });
  });
});
```

### 8.3 test.each를 활용한 파라미터화 테스트

```javascript
describe('getGrade', () => {
  test.each([
    [95, 'A'],
    [85, 'B'],
    [75, 'C'],
    [50, 'F']
  ])('점수 %i일 때 등급 %s 반환', (score, expectedGrade) => {
    expect(getGrade(score)).toBe(expectedGrade);
  });
});

// 객체 형태
test.each([
  { input: -1, expected: false },
  { input: 0, expected: true },
  { input: 120, expected: true },
  { input: 121, expected: false }
])('나이 $input의 유효성: $expected', ({ input, expected }) => {
  expect(isValidAge(input)).toBe(expected);
});
```

---

## 9. 단위 테스트 체크리스트

- [ ] JSDoc에 **대상**(파일:라인 - 함수명)이 명시되어 있는가?
- [ ] JSDoc에 **의도**(무엇을 검증하는지)가 명시되어 있는가?
- [ ] 소스 코드에 **테스트 위치 주석**(`@Test ...`)이 추가되었는가?
- [ ] **Arrange-Act-Assert 패턴**을 따르고 있는가?
- [ ] 재사용 설정은 **beforeEach**로 분리되어 있는가?
- [ ] 테스트명이 `{함수명} - {시나리오}` 형식인가?
- [ ] 관련 테스트가 **describe**로 그룹화되어 있는가?

---

# Part 3: 통합 테스트 규칙

## 10. 통합 테스트란?

### 10.1 단위 테스트 vs 통합 테스트

| 구분 | 단위 테스트 | 통합 테스트 |
|:-----|:-----------|:-----------|
| **범위** | 함수/클래스 하나 | 여러 모듈/컴포넌트 조합 |
| **의존성** | Mock 사용 | 실제 의존성 사용 |
| **속도** | 빠름 (< 1초) | 느림 (수 초 ~ 수십 초) |

### 10.2 Express 통합 테스트

```javascript
// tests/integration/userApi.integration.test.js
const request = require('supertest');
const app = require('../../src/app');
const db = require('../../src/db');

describe('User API 통합 테스트', () => {
  beforeAll(async () => {
    await db.connect();
  });

  afterAll(async () => {
    await db.disconnect();
  });

  beforeEach(async () => {
    await db.clear();
  });

  /**
   * 통합 테스트 ID: INT-001
   * 대상 흐름: POST /api/users → DB 저장 → GET /api/users/:id
   */
  test('사용자 생성 후 조회', async () => {
    // 1. 사용자 생성
    const createResponse = await request(app)
      .post('/api/users')
      .send({ name: '홍길동', email: 'hong@example.com' })
      .expect(201);

    const userId = createResponse.body.id;

    // 2. 사용자 조회
    const getResponse = await request(app)
      .get(`/api/users/${userId}`)
      .expect(200);

    expect(getResponse.body.name).toBe('홍길동');
  });
});
```

---

## 11. 통합 테스트 작성 규칙

### 11.1 파일 구조

```
tests/
├── unit/                     # 단위 테스트
│   ├── calculator.test.js
│   └── userService.test.js
│
├── integration/              # 통합 테스트
│   ├── setup.js             # 테스트 환경 설정
│   ├── userApi.integration.test.js
│   └── orderWorkflow.integration.test.js
│
└── e2e/                      # E2E 테스트
```

### 11.2 네이밍 규칙

**파일명**: `{대상}.integration.test.js`
- `userApi.integration.test.js`
- `orderWorkflow.integration.test.js`

**테스트명**: `{시작} → {끝} - {시나리오}`
- `사용자 생성 → 조회 - 정상 흐름`
- `주문 생성 → 결제 - 유효한 카드`

---

## 12. 통합 테스트 체크리스트

- [ ] JSDoc에 **통합 테스트 ID** (INT-XXX)가 명시되어 있는가?
- [ ] JSDoc에 **대상 흐름**이 명시되어 있는가?
- [ ] `tests/integration/` 디렉토리에 위치해 있는가?
- [ ] 파일명이 `{대상}.integration.test.js` 형식인가?
- [ ] 테스트 간 데이터 격리가 되어 있는가?

---

# Part 4: E2E 테스트 규칙

## 13. E2E 테스트란?

**사용자 관점에서 전체 시스템**을 검증하는 테스트다.

### 13.1 JavaScript E2E 테스트 도구

| 도구 | 용도 | 특징 |
|:-----|:-----|:-----|
| **Playwright** | 브라우저 E2E 테스트 | 크로스 브라우저, 빠름 |
| **Cypress** | 브라우저 E2E 테스트 | 디버깅 용이, 시각적 |
| **Puppeteer** | 헤드리스 브라우저 | Chrome 전용 |

---

## 14. Playwright E2E 테스트

```javascript
// tests/e2e/userJourney.spec.js
const { test, expect } = require('@playwright/test');

/**
 * E2E 테스트 ID: E2E-001
 * 시나리오: 사용자 회원가입 → 로그인 → 프로필 확인
 *
 * 검증 단계:
 * 1. 회원가입 폼 입력 및 제출
 * 2. 로그인 성공 확인
 * 3. 프로필 페이지에서 이름 표시 확인
 *
 * 사전 조건: 테스트 환경 서버 실행 중
 * 예상 결과: 프로필 페이지에 사용자 이름 표시
 */
test('사용자 가입 후 로그인 흐름', async ({ page }) => {
  // 1. 회원가입
  await page.goto('/signup');
  await page.fill('#email', 'test@example.com');
  await page.fill('#password', 'password123');
  await page.fill('#name', '홍길동');
  await page.click('#submit');

  // 2. 로그인 확인
  await expect(page.locator('#welcome-message')).toBeVisible();

  // 3. 프로필 확인
  await page.goto('/profile');
  await expect(page.locator('#user-name')).toHaveText('홍길동');
});
```

---

## 15. E2E 테스트 작성 규칙

### 15.1 파일 구조

```
tests/
├── unit/
├── integration/
└── e2e/
    ├── fixtures/            # 테스트 데이터
    ├── pages/               # Page Object Model
    │   ├── loginPage.js
    │   └── profilePage.js
    ├── userJourney.spec.js
    └── orderFlow.spec.js
```

### 15.2 네이밍 규칙

**파일명**: `{시나리오}.spec.js`
- `userJourney.spec.js`
- `orderFlow.spec.js`

**테스트명**: `{시나리오} - {결과}`
- `사용자 가입 후 로그인 흐름`
- `장바구니 추가 후 결제 완료`

### 15.3 Page Object Model

```javascript
// tests/e2e/pages/loginPage.js
class LoginPage {
  constructor(page) {
    this.page = page;
    this.emailInput = page.locator('#email');
    this.passwordInput = page.locator('#password');
    this.submitButton = page.locator('#submit');
  }

  async goto() {
    await this.page.goto('/login');
  }

  async login(email, password) {
    await this.emailInput.fill(email);
    await this.passwordInput.fill(password);
    await this.submitButton.click();
  }
}

module.exports = { LoginPage };

// tests/e2e/login.spec.js
const { LoginPage } = require('./pages/loginPage');

test('로그인 성공', async ({ page }) => {
  const loginPage = new LoginPage(page);
  await loginPage.goto();
  await loginPage.login('test@example.com', 'password123');

  await expect(page.locator('#dashboard')).toBeVisible();
});
```

---

## 16. 테스트 실행

```bash
# 전체 테스트 실행
npm test

# 단위 테스트만 실행
npm test -- --testPathPattern=unit

# 통합 테스트만 실행
npm test -- --testPathPattern=integration

# E2E 테스트 실행 (Playwright)
npx playwright test

# 특정 테스트 파일 실행
npm test -- calculator.test.js

# 커버리지 포함 실행
npm test -- --coverage
```

---

## 17. E2E 테스트 체크리스트

- [ ] JSDoc에 **E2E 테스트 ID** (E2E-XXX)가 명시되어 있는가?
- [ ] JSDoc에 **시나리오**가 사용자 관점으로 작성되어 있는가?
- [ ] JSDoc에 **검증 단계**가 순서대로 명시되어 있는가?
- [ ] JSDoc에 **사전 조건**과 **예상 결과**가 명시되어 있는가?
- [ ] `tests/e2e/` 디렉토리에 위치해 있는가?
- [ ] Page Object Model이 적용되어 있는가?
- [ ] 테스트가 **독립적으로 실행 가능**한가?

---

## 18. 의존성 설정 (package.json)

```json
{
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "@playwright/test": "^1.40.0"
  },
  "scripts": {
    "test": "jest",
    "test:unit": "jest --testPathPattern=unit",
    "test:integration": "jest --testPathPattern=integration",
    "test:e2e": "playwright test",
    "test:coverage": "jest --coverage"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageDirectory": "coverage",
    "collectCoverageFrom": [
      "src/**/*.js",
      "!src/**/*.test.js"
    ]
  }
}
```

---
